name: TRION Protocol CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  test-python:
    name: Python — Full Test Suite (416 tests)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install \
            pytest numpy scipy scikit-learn \
            flask flask-sqlalchemy requests httpx \
            faiss-cpu pydantic web3 \
            apscheduler feedparser langdetect \
            vadersentiment

      - name: Run Five Planes test suite
        run: python3 -m pytest tests/test_all_planes.py -v --tb=short

      - name: Run chain integration tests
        run: python3 -m pytest tests/test_chain_integrations.py -v --tb=short

      - name: Run whitepaper gap tests
        run: python3 -m pytest tests/test_whitepaper_gaps.py -v --tb=short

      - name: Run deep VM and 0G tests
        run: python3 -m pytest tests/test_deep_vm_and_zg.py -v --tb=short

      - name: Run vision expansion tests
        run: python3 -m pytest tests/test_vision_expansion.py -v --tb=short

      - name: Run TRION protocol unit tests
        run: python3 -m pytest tests/trion_protocol/ -v --tb=short

      - name: Run attack simulator
        run: python3 scripts/simulate_attacks.py

  compile-solidity:
    name: Solidity — Hardhat Compile
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Compile contracts
        run: npx hardhat compile

  compile-rust:
    name: Rust — Indexer Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      - name: Build Rust indexers
        run: cargo build --manifest-path rust-indexers/Cargo.toml

  stress-test:
    name: Stress Test — 200 req @ 226 req/s
    runs-on: ubuntu-latest
    needs: [test-python]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests numpy flask flask-sqlalchemy faiss-cpu scikit-learn

      - name: Run stress test
        run: python3 scripts/stress_test.py --ci
        continue-on-error: true

  deploy:
    name: Deploy — Trigger Production Deploy
    runs-on: ubuntu-latest
    needs: [test-python, compile-solidity]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Trigger Render deploy webhook
        run: |
          HTTP_STATUS=$(curl -s -o /tmp/render_response.txt -w "%{http_code}" \
            -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}")
          BODY=$(cat /tmp/render_response.txt)
          echo "Render response (HTTP $HTTP_STATUS): $BODY"
          if [ "$HTTP_STATUS" -lt 200 ] || [ "$HTTP_STATUS" -ge 300 ]; then
            echo "Deploy trigger failed with HTTP $HTTP_STATUS"
            exit 1
          fi
          echo "Deploy triggered successfully"
