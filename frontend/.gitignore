# Incremental TypeScript build cache (regenerated by tsc/next build; the
# earlier pattern "frontend/tsconfig.tsbuildinfo" never matched this file —
# a .gitignore pattern is relative to the .gitignore's own directory, so the
# double prefix pointed at frontend/frontend/tsconfig.tsbuildinfo — which is
# why the cache was accidentally committed in the first place). W4-Q fix.
tsconfig.tsbuildinfo
