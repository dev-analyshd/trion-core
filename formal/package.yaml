name: trion-formal
version: 1.0.0
synopsis: TRION Protocol formal verification — 9 theorems as types
license: CC0

dependencies:
  - base >= 4.14 && < 5

executables:
  trion-verify:
    main: Theorems.hs
    source-dirs: src/TRION

tests:
  spec:
    main: Spec.hs
    source-dirs: test
    dependencies:
      - hspec
