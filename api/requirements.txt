flask==3.1.3  # PHASE-1-SECURITY: bump 3.1.0→3.1.3 (PYSEC-2026-1377, PYSEC-2026-2151)
gunicorn==23.0.0
flask-socketio>=5.6.1
simple-websocket>=1.1.0
web3==7.15.0
requests>=2.33.0
numpy>=2.4.3
scipy>=1.15.0
scikit-learn>=1.8.0
httpx>=0.28.1
psycopg2-binary>=2.9.11
apscheduler>=3.11.2
vaderSentiment>=3.3.2
feedparser>=6.0.12
langdetect>=1.0.9
stellar-sdk>=13.2.1
tronpy>=0.6.2
cosmpy>=0.11.2
python-dotenv>=1.0.0
flask-cors>=4.0.0
# PHASE-1-SECURITY: transitive dep overrides (pin minimum safe versions)
# PHASE-1-SECURITY: pynacl (transitive via cosmpy) is pinned by cosmpy==0.12.2
# to pynacl==1.6.0 which has PYSEC-2026-3002. We force-upgrade to 1.6.2 via
# `pip install --no-deps pynacl==1.6.2` in the Dockerfile (post-install).
# pynacl 1.6.0 → 1.6.2 is a patch bump with no API breaks.
# pynacl==1.6.2  # (installed in Dockerfile via --no-deps after main install)
# ecdsa (0.19.2) has advisory PYSEC-2026-1325 with NO upstream fix yet.
# Pinning current version to surface it; monitor for upstream release.
ecdsa==0.19.2  # NO FIX AVAILABLE; revisit when upstream releases patch
