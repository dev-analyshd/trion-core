[package]
name = "btcp_escrow"
version = "0.1.0"
description = "TRION BTCP Escrow - Two-State Atomic Escrow for Cross-Chain Settlement"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
no-entrypoint = []
test-sbf = []

[dependencies]
solana-program = "1.18"
borsh = "1.5"
thiserror = "1.0"
