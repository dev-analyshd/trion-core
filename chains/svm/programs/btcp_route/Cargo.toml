[package]
name = "btcp_route"
version = "0.1.0"
description = "TRION BTCP Route - Cross-Chain Route Computation and Verification"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
no-entrypoint = []
test-sbf = []

[dependencies]
solana-program = "1.18"
borsh = "1.5"
thiserror = "1.0"
