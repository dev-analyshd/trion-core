# Risk Register — Remaining Risks After the Fix Waves

**Task ID:** 9-c · **Write time:** 2026-09-05, after fix waves 7-a…7-f / 8-a…8-c and a parallel lane (cold-start/CEX-forward/indexer-remainder fixes — observed in the working tree at write time, regression tests re-run green by this agent). **Refreshed 10-b** after waves 9-a…10-b (R-15 closure, R-20…R-22 additions, final-suite state) — then **independently re-verified by the 10-b final-verification pass**: the full-suite re-run reproduced **1831P / 2F / 27S / 1x / 1E identically** (485.55s vs the original 463.16s), the tron `blockID` fix re-checked (field match vs `fetch_tron_block` + brace balance 58/58·47/47·216/216), the two-chain BH-ledger experiment re-run (`per_chain {solana: 1, ethereum: 1}` — distinct tx hashes; a shared tx hash dedups by design), and R-22's grep re-confirmed (0 X-API-Key matches under core/{trading,agent,auditor,akashic}). Fix-wave changes were committed by **10-c** as a 9-commit hardening series and pushed to origin/main (R-19 closed). **Refreshed 12-b after Wave 5 (Tasks 11-a…11-d):** R-14 / R-16 / R-20 / R-21 / R-22 closed (see §2 rows, CLOSED status); M-073 resolved by the owner's 29-type signal-taxonomy ruling (REQUIREMENTS_MATRIX). **Finalized 13-a (Wave 7):** the waves 5–7 full-suite verification ran with live services (FAISS :8000 + Flask :5000, keyed) — **1907 passed / 0 failed / 0 errors / 28 skipped / 1 xfailed, 1936 collected, exit code 0**, reproduced identically across two independent runs (236.02s / 231.62s): both 10-b flakes resolved (beo coverage self-sufficient PASS, live-ingestion GitHub leg honest rate-limit skip), R-17 closed (supervisor committed at HEAD), the dead-code cleanup (12-a) confirmed with zero test impact, and waves 5–7 verified to touch **NO Rust paths** (`git status --short | grep -E 'indexers|rust/'` → empty) — R-01's scope is exactly the waves 1–4 Rust edits. Open items below are unchanged except as annotated.

Risk IDs below are this register's own (R-*); SEC-* refer to SECURITY_AUDIT.md; M-* to REQUIREMENTS_MATRIX.md.

---

## 1. Externally-blocked (cannot close inside the repo/sandbox)

| ID | Risk | Blocks | Evidence / gate |
|---|---|---|---|
| R-01 | **cargo verification gap** — the waves 1–4 Rust edits (5 indexer hash fixes + hedera + event-byte fixes + botchain regression module + faiss.rs key headers + the parallel lane's 6 more crates + the tron `blockID` fix) are hand-verified only; no cargo build/test ever ran | indexers/, rust/ trion-btcp, contracts/{cosmwasm,svm,ink} | no cargo in sandbox (worklog Tasks 3/7-d/8-a/8-b); mandatory follow-up: `cargo check && cargo test` in indexers/ and rust/. **Wave-7 note (13-a): waves 5–7 (Tasks 11-a…12-c) touched NO Rust code** — verified on the final tree: `git status --short | grep -E 'indexers|rust/'` → empty (the 11-c/12-c changes were Python/TS/Vyper/Cairo; 12-a's deletions were a Go file + package.json script entries + docs) — so **R-01's scope is exactly the waves 1–4 changes**; cairo is not in this gap (R-16 closed by 11-b with scarb 2.8.4 + 2.10.1 builds) |
| R-02 | **Go verification gap** — validator/ + network/ statically verified only (36 tests never executed here) | validator fleet software | no go toolchain in sandbox |
| R-03 | **ZK trusted-setup ceremony** — no zkeys/r1cs/ptau/verifiers; circuits unverifiable (SEC-08) | M-145/209/211, sensing-oracle ZK leg, IAP privacy | requires real Powers-of-Tau MPC — external coordination |
| R-04 | **Validator fleet** — no live validators; emission is single-signature relayer (SEC-07); certificate emission-side signing has no producer | M-184, M-185, M-247–M-252, M-288, falsification windows, quorum reality | registry launch gate exists; fleet is operational work |
| R-05 | **Funded custody / live value legs** — no funded escrows; BTC legs simulated; escrow-bound cert fix is test-proven, not value-proven | any mainnet escrow release | MAINNET_RUNBOOK gates |
| R-06 | **Professional audit** — runbook REQUIRES it before mainnet; this session is an internal audit, not a substitute | mainnet (M-288) | docs/MAINNET_RUNBOOK.md gates: audit, 6-month observation-only, ≥100 validators, INIT ceremony, fresh deployer key |
| R-07 | **Live-chain legs** — indexer RPC streams, relayer submissions, deployments are testnet/self-reported; the "34,600 vectors / deployed contracts" figures are self-reported | any live-operations claim | deployment records "unverified — self-reported"; one fabricated Solana devnet record was purged historically |

## 2. Known residuals (open, in-repo)

| ID | Risk | Status at write time | Evidence |
|---|---|---|---|
| R-08 | **SEC-15 delivery-time SSRF pinning** — webhook registration resolves + rejects private/non-global IPs, but each delivery still resolves through the egress path (DNS-rebinding at delivery time) | OPEN (documented in-code; egress-proxy owned) | api/cex_integration.py `_resolve_webhook_ips` + residual comment |
| R-09 | **FAISS rate limiting** — auth closed the poisoning paths; no request-rate limiter on the FAISS service (Flask has 300/60s/IP) | OPEN | SECURITY_AUDIT.md SEC-01 residual |
| R-10 | **SEC-24 message scope** — PQC signing endpoint authenticated but signs caller-supplied payloads (no type-tagged domain separation) | OPEN (keyed callers only; ephemeral key) | faiss_service.py:5731 |
| R-11 | **SEC-26 Bug 2 availability** — fork/resurrection/convergence endpoints 0% under 60s load (event-loop blocking, ~13 rps write ceiling) | OPEN | tests/unit/ANIMA_STRESS_REPORT.md |
| R-12 | **SEC-12 TON u64 unit hazard** — whole-unit ledger vs DECIMALS=18 metadata; caller-enforced invariant | OPEN (TRUTHFUL-NOTE) | contracts/ton/token.fc:19-30 |
| R-13 | **SEC-07 single-sig relayer** — one key = unilateral publication on quorum-1 chains | OPEN (honest label; 7-of-12 multisig is the pre-mainnet fix) | relayer/relayer.js:16-27 |
| R-14 | **Python TON streamer tip-hash anchoring** — Python fetcher anchored ALL seqnos to the tip's root_hash; Rust used per-seqno hashes → Rust↔Python TON digests matched only for the tip block | **CLOSED 11-c (Wave 5):** `fetch_ton_block` now issues per-seqno `GET /getBlockHeader?workchain=-1&shard=8000000000000000&seqno={seq}` (the exact toncenter v2 call the Rust trion-ton crate uses) and takes `result.root_hash` verbatim, ok-gated; missing → stderr warn + honest "0x0" (CANONICAL_BH §9); the tip-root getMasterchainInfo stamping is gone | tests/unit/test_bh_streamer_fetchers.py (6P — TON per-seqno URL exact, distinct seqnos→distinct hashes, missing root_hash → warn+"0x0", API error → "0x0"); worklog 11-c |
| R-16 | **Cairo contracts/starknet crate corelib skew** — 34 pre-existing corelib-skew errors in 4 files: trion_certificate 17, btcp_escrow 15, BTCFiGuard 1, trion_epoch_registry 1 (felt PartialOrd, u32/u64 mismatches; corelib version skew) | **CLOSED 11-b (Wave 5):** all 4 files migrated to current corelib (`core::poseidon::poseidon_hash_span`, `core::ecdsa::check_ecdsa_signature`, `felt_lt()` u256-backed range checks, `crate::` paths, generate-trait impl form) + byte-identical twins synced in contracts/cairo + trion_execution_gate.cairo; both crates build CLEAN to sierra+casm under scarb 2.8.4 AND 2.10.1 (4/4 — the repo's pinned toolchains); the 34 errors (certificate 17, escrow 15, guard 1, registry 1) are all closed; mirror tests re-pinned | worklog 11-b; tests/contracts/ 69P/0F (incl. twin identity + static source pins); cairo-test runtime integration run remains a documented pre-mainnet follow-up |
| R-17 | **evm_extras supervisor gap** — test_evm_extras_supervisor_has_all_three_chains failed at the 7-d baseline (BNB_TESTNET missing from supervisors/evm_extras_indexers.sh) | **CLOSED (observed 10-b, confirmed 13-a):** the gap was closed by a session lane and committed — HEAD's `supervisors/evm_extras_indexers.sh` includes BNB Testnet (line 4) — and the test PASSED in the 10-b final runs and in both 13-a runs (zero failures) | git show HEAD:supervisors/evm_extras_indexers.sh; worklog 7-d (baseline), 10-b |
| R-18 | **INV-015 dispute window** — the repo's only xfail: on-chain 72h dispute window unenforceable | OPEN (honest) | tests, M-186 evidence |
| R-19 | **Commit closed (10-c)** — the session's hardening series has been committed (9 commits: faiss auth, api-side keys, flask surface, birp attestations, escrow binding, real block hashes, silence/provenance, private-key printing, master-audit docs) and pushed to origin/main; the 10-b tron §9 fix is included — still cargo-unverified until a `cargo check` run (R-01) | CLOSED 10-c — commits landed on origin/main on top of c0ccb14, working tree clean after push. **Wave-7 note (13-a, resolved 13-b):** the waves 5–7 changes (54 paths: signal taxonomy, cairo corelib, integrity residuals, API alignment, vyper staking, dead-code deletions, docs refresh) were committed by 13-b as an 11-commit series on top of af5317b and pushed to origin/main — remote HEAD verified to match local after the push, working tree clean | git log origin/main; git status (13-b) |
| R-20 | **CEX BH construction diverged from canonical L0.1 recomputation** — the CEX lane built its own sense/antisense pair (best-effort forward thread) instead of the canonical 93-byte payload + dual-strand SHA3-256; the ledger's `verified` complementarity counter stayed 0 for CEX-sourced records | **CLOSED 11-c (Wave 5):** api/cex_integration.py construction rebuilt canonical — payload packs `int(clamp(mag)*1e9)` at the canonical 1e9 nano scale + 8 ZERO context bytes (ts/chain/block_hash/entity derivations already byte-identical); unrounded magnitude_norm forwarded. LIVE PROOF (keyed FAISS :8931 + Flask test_client): unkeyed POST → 401 fail-closed; real `/api/v1/cex/ingest` (3 records: BUY 850k, SELL 1.25M, wash-flagged 60k) → forward 200 `stored:3, verified:3` — the counter 9-a saw stuck at 0 is 3/3; rows in /bh/ledger + /bh/stats per_chain CEX_BINANCE: 3 | tests/unit/test_cex_faiss_forward.py TestCanonicalL01Verification (4 new tests, importing the endpoint's own verify_bh_complementarity); worklog 11-c. Follow-up flagged (CEX owner, out of R-20 scope): the module's EVENT_TYPES byte→name display map is stale vs the canonical 20 |
| R-21 | **Python cosmos fetcher parent-hash drift** — bh_streamer.py read `header.last_block_id.hash` (the PARENT block) where the Rust trion-cosmos crate reads `block_id.hash` (current block) → the same cosmos block hashed differently on the two paths | **CLOSED 11-c (Wave 5):** `fetch_cosmos_block` now reads the CURRENT block's `result.block_id.hash` with the .get() "0x0" fallback — the same field trion-cosmos reads from LCD /blocks/{h} | tests/unit/test_bh_streamer_fetchers.py (6P — current-block_id.hash vs parent, "0x0" fallback); worklog 11-c |
| R-22 | **core/-side internal FAISS clients without key/env integration** — 5 unkeyed callers against a keyed service fail closed (safe posture, non-functional); off the pytest import path (untested) | **CLOSED 11-c (Wave 5):** all FIVE callers keyed with LOCAL helpers (same 3-var order FAISS_API_KEY → FAISS_SERVICE_API_KEY → TRION_API_KEY, blank → no header; zero core→api imports): core/akashic/genesis.py (POST /archetypes/match_vector), core/price/behavioral_price_engine.py (GET /bh/stats), core/auditor/contract_auditor.py (GET /api/v1/planes/{addr}/physical), core/trading/live_feed.py + core/trading/signal_engine.py (httpx GETs, key resolved once at construction — FAISSAccumulator pattern) | NEW tests/unit/test_core_faiss_auth.py (10P: resolution order, blank→no-header, keyed GET/POST with captured headers, key-resolved-once); worklog 11-c |

### Closed during this session (recorded so future readers don't re-flag)

| Was | Closed by | Verification |
|---|---|---|
| **R-15 — SWEEP-B §9 crate arms (incl. Python APTOS stale key)** — the Python aptos fetcher's stale `previous_block_hash` key (silently degraded every block to "0x0") AND the last unassigned Rust §9 arm | 10-a (Python fetcher now reads the live `block_hash` field + `_synthetic_tx_sender` single-hash fix) and 10-b (trion-tron `blockID` verbatim, missing → warn + "0x0", synthetic `bh_id("tron_block:…")` deleted — the 21st/last crate) | golden + chain_registry 150P (10-a); field cross-check vs `fetch_tron_block`'s `blockID` read + lifetime/char/string-aware brace-balance check (10-b, re-verified: `{`=58/`}`=58, `[`=47/`]`=47, `(`=216/`)`=216); SWEEP-B §1 row, Wave-4 header, D3 all closed. **Cargo gap remains** (R-01 — toolchain absent in every wave, all 21 crate fixes static-verified only); the fix is committed and pushed (R-19 closed by 10-c) |
| CEX→FAISS forward schema drift (silent 422 since forever; 8-a flagged) | parallel lane (write-time observation) | _forward_to_faiss posts TxBhBatchPayload; tests/unit/test_cex_faiss_forward.py 5P (re-run by this agent) |
| Cold-start signal-route 500s (KeyError plane_breakdown; GOVERNANCE h[33] IndexError; 8-c flagged) | parallel lane | tests/unit/test_api_cold_start.py 8P (re-run by this agent) |
| transduction_integrity unkeyed FAISS calls (8-a follow-up) | parallel lane | `_faiss_headers()` :39 + attachments :159/:171/:194 |
| self_verification_routes FAISS_URL alias-only resolution (env drift) | parallel lane | FAISS_SERVICE_URL precedence in the working tree; tests/unit/test_self_verification_auth.py 11P (re-run by this agent) |
| SWEEP-B D3 remainder + vechain/algorand/cardano re-encode class (8-b flags) | parallel lane | 6 crates now verbatim-hash; golden + registry 150P re-run |
| **Dead-code cleanup — FILE_DISPOSITION §22 proven deletions** (validator/cmd/trion-validator/crawler_coordinator.go, chains/near/deploy_wasm.cjs, 12 dead package.json script entries across 6 chains/*/package.json; FILE_DISPOSITION §2 converted into a disposition ledger; 1 recommendation voided) | 12-a (Wave 6) | zero test impact: tests/unit **1136P / 6S / 0F identical at baseline and after every deletion batch**; confirmed by the 13-a final full suite (1907P/0F/0E) — CHANGELOG metrics fixed in the same pass (129 total / 40 integrated, 29-type ruling) |

## 3. Contradiction register (pointers)

- **Repo-internal K1–K22 (+ R-BH-01, R-SG-05, R-CH-02, R-TK-01):** docs/audit/CANONICAL_SPEC_MATRIX.md — 22 recorded spec-vs-code conflicts resolved by the canonical reconstruction; spec-hygiene pass still recommended (worklog item 10).
- **New this session (Task 5 matrix): 19 doc-vs-doc contradictions** — REQUIREMENTS_MATRIX.md §CONTRADICTORY (M-002, M-006, M-008, M-021, M-053, M-054, M-059, M-072, M-073, M-110, M-168, M-172, M-181, M-195, M-203, M-233, M-277, M-293, M-296). 18 remain unresolved — they are documentation-level; a spec reconciliation pass is the only closure path. **M-073 left the register in Wave 5 (owner ruling: 29-type canonical taxonomy — 19 base + 10 BTCP-family, closed set 27 distinct names; implemented + pinned by Task 11-a, see REQUIREMENTS_MATRIX.md).**

## 4. Structural (not bugs — permanent until the fleet exists)

- Bootstrap planes Σ/K/A fixed values in some emission paths (M-110) — disclosed, engines real.
- TimescaleDB 17/35 declaration-only (M-215).
- Falsification conditions F1–F15 designed-not-operating (fleet-gated; matrix domain 13).
- ANIMA sub-scale (59 languages / 36 sources vs 1000-crawler spec).
- The four "proofs" are prose; Haskell Theorems is the strongest formal artifact.
- Governance vote/timelock mechanics (M-270/M-272) unimplemented.

## 5. Highest-leverage next actions (owner: coordinator)

1. ~~Run the final full pytest suite~~ **DONE (10-b, re-verified; FINALIZED 13-a):** the waves 5–7 tree with FAISS+Flask live → **1907P / 0F / 28S / 1x / 0E, exit code 0** (1936 collected, 236.02s + 231.62s reproduction) — zero failures/errors, zero session regressions, both 10-b flakes resolved (beo self-sufficient PASS via 11-d; live-ingestion GitHub leg honest rate-limit skip via 11-d), +74 wave-5–7 tests all green (FINAL_TEST_REPORT.md final section). ~~Commit the session (R-19)~~ DONE for waves 1–4 (10-c — committed and pushed); **waves 5–7 (54 paths) await the coordinator's commit**.
2. `cargo check && cargo test` in indexers/ + rust/ (R-01) — the single largest unverified surface the session touched. **Waves 5–7 touched NO Rust paths (13-a verification: `git status --short | grep -E 'indexers|rust/'` → empty) — the gap is exactly the waves 1–4 edits; cairo is out of it (R-16 closed).**
3. ~~Cairo corelib migration pass for contracts/starknet (R-16)~~ **DONE (11-b, Wave 5):** both cairo crates build clean under scarb 2.8.4 AND 2.10.1; tests/contracts 69P/0F; cairo-test runtime integration remains the pre-mainnet follow-up.
4. Spec reconciliation pass for the 18 remaining doc contradictions + K1–K22 hygiene (owner: docs conformance). M-073 is resolved (owner ruling, 11-a) and recorded in REQUIREMENTS_MATRIX.md / spec/signal_types.md.
5. Fleet/ceremony/audit/funding gates per the runbook (R-03…R-07) — the only path to L10.
