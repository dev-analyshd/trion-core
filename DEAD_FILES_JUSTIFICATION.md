# Dead Files Justification

**Date:** 2026-09-14
**Identity:** dev-analyshd

---

## Removed from Git Tracking

### Build Artifacts (removed, gitignored)

| File | Reason |
|------|--------|
| `formal/src/TRION/Theorems.o` | Haskell build artifact (.o). Regenerated by `stack build`. |
| `formal/app/Main.o` | Haskell build artifact (.o). Regenerated by `stack build`. |
| `.pytest_cache/` | Python test cache. Regenerated by `pytest`. |

### Database Files (removed, gitignored)

| File | Reason |
|------|--------|
| `akashic_state.db` | SQLite local state. Regenerated at runtime. |
| `akashic_state.db-shm` | SQLite WAL shared memory. Runtime-only. |
| `akashic_state.db-wal` | SQLite WAL log. Runtime-only. |
| `bh_ledger.db` | SQLite behavioral hash ledger. Regenerated by bh_streamer. |
| `cex_bh_ledger.db` | SQLite CEX BH ledger. Regenerated at runtime. |
| `self_verification.db` | SQLite self-verification cache. Runtime-only. |
| `akashic/bibl_patterns.db` | Akashic pattern DB. Runtime-only. |
| `akashic/crispr_adaptive.db` | CRISPR adaptive DB. Runtime-only. |
| `akashic/epigenetic_immunity.db` | Epigenetic immunity DB. Runtime-only. |
| `db/btcp_state.db` | BTCP state DB. Runtime-only. |

### Compiled Contract Binaries (KEPT — deployment artifacts)

The `contracts/solidity/compiled/*.bin` files are **kept** because they are
deployment artifacts with provenance value — they record which bytecode was
deployed on-chain.

---

## Archived (not deleted — evidence retained with provenance)

### Superseded Mission Evidence

All proof/evidence files have been moved from `docs/proofs/` to the
`proofs/` directory tree with categorical organization. No evidence was
deleted. The `docs/proofs/` directory is now empty (will be removed).

### Retraction Notes

The ZK retraction note (`docs/zk_starknet/`) is **retained in place**
as it documents a historical claim retraction per the evidence culture policy.

---

## Empty Directories

Empty directories in `proofs/` (created during reorganization for future
use) are **retained** — they represent the target structure for future
proof submissions.

---

## Summary

| Category | Action | Count |
|----------|--------|-------|
| Build artifacts | Removed from git, gitignored | 3 |
| Database files | Removed from git, gitignored | 10 |
| Evidence files | Moved with provenance (never deleted) | 68 |
| Empty dirs | Retained (future structure) | 14 |

No evidence files were deleted. All removals are build artifacts or
runtime databases that are regenerated automatically.
