[workspace]
resolver = "2"
members = [
    "btc-spv-verifier",
    "btcp-escrow",
    "btcp-intent",
    "btcp-route",
    "liquidity-ocean",
]

[workspace.dependencies]
soroban-sdk = "22"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
codegen-units = 1
