[package]
name = "btcp_escrow"
version = "0.1.0"
description = "TRION BTCP Escrow — Two-State Atomic Escrow for Solana"
edition = "2021"
license = "MIT"

[dependencies]
anchor-lang = "0.29.0"
btcp_common = { path = "../btcp_common" }

[lib]
crate-type = ["cdylib", "lib"]
name = "btcp_escrow"
