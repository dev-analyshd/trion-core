[package]
name = "btcp_route"
version = "0.1.0"
description = "TRION BTCP Route Proof Tracking for Solana"
edition = "2021"
license = "MIT"

[dependencies]
anchor-lang = "0.29.0"
btcp_common = { path = "../btcp_common" }

[lib]
crate-type = ["cdylib", "lib"]
name = "btcp_route"
